#!/usr/bin/env node
var debug = require('debug')('ScratchNode');
var api = require('../api');

api.set('port', process.env.PORT || 3000);

var server = api.listen(api.get('port'), function() {
  debug('Express server listening on port ' + server.address().port);
});
